name: Run Shell Scripts

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Shell Environment
      run: |
        sudo apt-get update
        echo "Set up completed"
     
    - name: Make Shell Script Executable
      run: |
        chmod +x ./path/to/your/Fireflink-Client.sh

    - name: Run Shell Scripts
      run: |
        ./Fireflink-Client.sh
        ./Signin.sh

